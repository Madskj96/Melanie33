name: Publish birthday site

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is temporarily paused while the high-quality site package is uploaded."
